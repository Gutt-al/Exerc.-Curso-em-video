# ex007 Desenvolva um programa que leia as duas notas de um
# aluno, calcule e moestre a sua média.

A = float(input('Proof number one:'))
A1 = float(input('Proff number two:'))
m = (A+A1)
print('The two test add up {},the then avarege be: {:.1f}'.format(m, (m/2)))
